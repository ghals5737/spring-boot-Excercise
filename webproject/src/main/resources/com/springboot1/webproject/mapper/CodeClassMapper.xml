<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="com.springboot1.webproject.mapper.CodeClassMapper">
     
     <insert id="create">
         INSERT INTO code_class
         (
           class_code,
           class_name
         )
         VALUES
         (
           #{classCode},
           #{className}
         )
     </insert>

     <select id="list" resultType="CodeClass">
         SELECT class_code,
           class_name,
           use_yn,
           reg_date
         FROM code_class
         ORDER BY class_code DESC,
           reg_date DESC  
     </select>
  </mapper>